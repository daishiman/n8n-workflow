# Step180: 配置最適化

## 目的

Step150で統合されたワークフローJSONに対して、ノードの視覚的配置を最適化し、可読性と保守性を最大化します。12層アーキテクチャとグループ構造を視覚的に表現する論理的な配置設計を実現します。

**最終成果物:**
- 配置最適化されたワークフローJSON（全ノードのposition更新済み）
- 配置設計ドキュメント（配置ロジックの説明）
- 視覚的レイアウト図（ASCII artまたは説明）

---

## 背景

### Step180が必要な理由

1. **可読性の向上**: 論理的な配置により、ワークフローの理解が容易になる
2. **保守性の向上**: グループとレイヤーが視覚的に明確になり、修正が簡単になる
3. **アーキテクチャの可視化**: 12層構造が一目で理解できる配置
4. **チーム協業の促進**: 統一された配置ルールにより、チームメンバー間の理解が統一される
5. **デバッグの効率化**: データフローが視覚的に追跡しやすい配置

### v4.0における配置最適化の位置づけ

```
Phase 3: 統合・検証・出力 (Step150-190)
├─ Step150: グループ間接続統合 ✅
├─ Step160: ワークフロー全体検証 ✅
├─ Step170: Sticky Note完成 ✅
├─ Step180: 配置最適化 ← 現在のステップ
└─ Step190: 最終出力
```

### 従来手法との違い

| 観点 | 従来手法 | v4.0 Step180 |
|------|---------|-------------|
| 配置方針 | ランダムまたは手動配置 | レイヤー・グループベースの体系的配置 |
| 可読性 | 配置者の感覚に依存 | 定量的な間隔・配置ルール適用 |
| スケーラビリティ | 大規模ワークフローで配置が困難 | 40グループでも論理的配置が可能 |
| 保守性 | 配置意図が不明確 | 配置設計ドキュメントで意図を明示 |

---

## 言葉の定義

### 配置関連用語

1. **レイヤー配置 (Layer Placement)**
   - 12層アーキテクチャ (L1-L12) に基づく垂直方向の配置
   - Y座標で表現され、上から下へデータフローを表現

2. **グループ配置 (Group Placement)**
   - 機能グループ（G001-G040）ごとの水平方向の配置
   - X座標で表現され、並列処理や独立した機能を左右に配置

3. **ノード間隔 (Node Spacing)**
   - 同一グループ内のノード間の垂直間隔: 推奨180px
   - 同一レイヤー内のグループ間の水平間隔: 推奨300px

4. **基準点 (Origin Point)**
   - 配置計算の起点座標: [100, 100]
   - Triggerノードまたは最初のグループの左上位置

5. **接続ライン (Connection Line)**
   - ノード間を結ぶ視覚的な線
   - 直線配置を優先し、交差を最小化

6. **Sticky Note配置 (Sticky Note Placement)**
   - グループタイトル用Sticky Noteの配置
   - グループの左上、ノードの上部に配置

### n8n座標系

```
(0, 0) ────────────→ X軸
  │
  │   [100, 100] ← 基準点
  │      │
  │      ▼
  │   ┌─────────┐
  │   │  Node   │
  │   └─────────┘
  ▼
 Y軸
```

- **X座標**: 左から右へ増加（水平位置）
- **Y座標**: 上から下へ増加（垂直位置）
- **推奨範囲**: X: 0-5000, Y: 0-10000

---

## 制約

### 配置設計の制約

1. **n8nキャンバス制約**
   - 座標系: [x, y] の2次元配列
   - 実用的な範囲: X: 0-5000, Y: 0-10000
   - ノードサイズ: 約 240px × 100px

2. **視覚的ベストプラクティス**
   - ノード間の最小間隔: 縦180px、横300px
   - レイヤー間の垂直間隔: 300px
   - グループ間の水平間隔: 400px

3. **可読性の制約**
   - 1レイヤーあたりの最大グループ数: 5グループ（水平方向）
   - 1グループあたりの最大ノード数: 8ノード（垂直方向）
   - 接続ラインの交差を最小化

4. **論理的制約**
   - データフローは上から下へ（Y座標増加方向）
   - 並列処理は左から右へ（X座標増加方向）
   - エラーフローは右側または下部に配置

5. **Sticky Note制約**
   - パターン1（全体フロー）は [100, 50] 付近に固定し、最低700px × 650px・色7（薄ピンク）で全文を表示
   - グループ用Sticky Noteは左上に配置し、最低520px × 420pxを確保。ノード数や記述量に応じてさらに拡張
   - `### 🔗 関連ノードブロック` で列挙するノードが視認できる位置に配置し、対象ノード群の外接矩形と重ならないようにする
   - color=0/1（白系）は使用禁止。全体フロー=7、メイングループ=6（薄オレンジ）、エラーフロー=5（薄赤）で統一し、一目で識別できるようにする
   - テキストはスクロールなしで全文が読める寸法に調整し、ノードブロックが途切れないことを確認する
   - Sticky Noteのcontentは Step170で生成済み、本ステップでは配置座標のみを決定

---

## 処理手順

### ステップ0: グループ整列配置戦略の決定

**目的**: 画像のような視覚的に綺麗な構成を実現するため、グループごとの整列配置戦略を決定

**背景**: グループ内のノードを整然と配置し、グループ間を視覚的に明確に分離することで、ワークフロー全体の可読性を最大化する

**処理内容**:

1. **グループ整列パターンの選定**

**パターンA: 横一列配置**（推奨: すべてのグループのデフォルト）
```
┌─────────────────────────────────────────────────────────────┐
│  Sticky Note: Group 1                                       │
│  ┌────┐   ┌────┐   ┌────┐   ┌────┐   ┌────┐               │
│  │Node│ → │Node│ → │Node│ → │Node│ → │Node│               │
│  │ 1  │   │ 2  │   │ 3  │   │ 4  │   │ 5  │               │
│  └────┘   └────┘   └────┘   └────┘   └────┘               │
└─────────────────────────────────────────────────────────────┘
```
- ノード間隔: 水平300px、垂直0px（同一Y座標）
- 適用: すべてのグループ（シーケンシャル・並列問わず）
- グループ全体が水平方向に展開され、視覚的に理解しやすい
- 例: トリガー → 検証 → 変換 → 出力のような直線的なフロー

**パターンB: AIエージェント階層配置**（推奨: AIエージェント関連ノード）
```
┌─────────────────────────────────────────────────────────────┐
│  Sticky Note: Group 2 (AI処理)                              │
│  ┌────┐   ┌────────┐   ┌────┐                              │
│  │前処│ → │AI Agent│ → │後処│                              │
│  │理  │   │        │   │理  │                              │
│  └────┘   └───┬────┘   └────┘                              │
│               │                                             │
│          ┌────┴─────┬──────────┬──────────┐                │
│          ↓          ↓          ↓          ↓                │
│       ┌──┴──┐   ┌──┴──┐   ┌──┴──┐   ┌──┴──┐              │
│       │Lang │   │Memo │   │Tool │   │Tool │              │
│       │Model│   │ry   │   │  1  │   │  2  │              │
│       └─────┘   └─────┘   └─────┘   └─────┘              │
└─────────────────────────────────────────────────────────────┘
```
- メインノード: 水平300px間隔、同一Y座標
- サブノード（Language Model, Memory, Tools等）: 親ノード（AI Agent）の下に配置
  - Y座標: 親ノード + 200px
  - X座標: 親ノードを中心に左右に展開（150px間隔）
- 適用: AI Agentノードとその依存ノード
- 階層的な依存関係が視覚的に明確

**パターンC: グリッド配置**（推奨: 複雑な分岐・並列処理）
```
┌───────────────────────────────┐
│ Sticky Note: Group 3          │
│  ┌────┐   ┌────┐              │
│  │Node│ → │Node│              │
│  │ 1  │   │ 2  │              │
│  └──┬─┘   └──┬─┘              │
│     ↓        ↓                │
│  ┌──┴─┐   ┌──┴─┐              │
│  │Node│   │Node│              │
│  │ 3  │   │ 4  │              │
│  └────┘   └────┘              │
└───────────────────────────────┘
```
- ノード間隔: 水平300px、垂直180px
- 適用: 複数の並列処理 + 後続処理

2. **グループ間分離戦略**

**垂直分離**（デフォルト: 上から下へグループ配置）
```
┌─────────────────────────────────────────┐  Y=100
│  Group 1: トリガー・入力受付              │
│  ┌────┐ → ┌────┐ → ┌────┐               │
│  │Tri │   │Val │   │Set │               │
│  └────┘   └────┘   └────┘               │
└─────────────────────────────────────────┘
                 ↓ 依存関係
┌─────────────────────────────────────────┐  Y=600
│  Group 2: データ変換・加工                │
│  ┌────┐ → ┌────┐ → ┌────┐               │
│  │Code│   │HTTP│   │JSON│               │
│  └────┘   └────┘   └────┘               │
└─────────────────────────────────────────┘
                 ↓ 依存関係
┌─────────────────────────────────────────┐  Y=1100
│  Group 3: AI処理                         │
│  ┌────┐ → ┌────┐ → ┌────┐               │
│  │Pre │   │AI  │   │Post│               │
│  └────┘   └─┬──┘   └────┘               │
│             └─ サブノード（下にぶら下げ） │
└─────────────────────────────────────────┘

グループ間隔: 500px（Sticky Note高さ + マージン）
- 適用: すべてのグループ（デフォルト配置）
- データフローの依存関係が上から下へ明確に表現される
- 各グループは独立した処理単位として視覚的に分離
```

**水平並列配置**（特殊ケース: 同一レイヤー内の並列処理）
```
                 ┌──────┐  Y=600
                 │Group │
                 │  1   │
                 └──┬───┘
        ┌─────────┼─────────┐
        ↓         ↓         ↓
  ┌──────┐  ┌──────┐  ┌──────┐  Y=1100
  │Group │  │Group │  │Group │
  │  2a  │  │  2b  │  │  2c  │
  └──┬───┘  └──┬───┘  └──┬───┘
      └────────┼─────────┘
               ↓
         ┌──────┐  Y=1600
         │Group │
         │  3   │
         └──────┘

水平間隔: 600px（各並列グループのSticky Note幅 + マージン）
垂直間隔: 500px（標準グループ間隔）
- 適用: 同一レイヤー内の並列処理のみ
- 例: 複数のAPIを並列呼び出し、結果を統合する場合
```

**エラーフロー分離**（右側または下部に独立配置）
```
┌────────────────────────────────────────────┐ Y=50
│  全体フロー Sticky Note（横長・緑色）      │
└────────────────────────────────────────────┘
        ↓
┌──────┐    ┌──────┐                       Y=400
│Group │    │Group │
│  1   │    │  2   │
└──────┘    └──────┘
        ↓
┌──────┐    ┌──────┐                       Y=900
│Group │    │Group │
│  3   │    │  4   │
└──────┘    └──────┘

- 全体フローは上部に横長配置
- グループは2×2グリッドで配置
- グループ間隔: 水平500px、垂直500px
```

3. **グループ配置マトリックスの作成**

各グループに整列パターンと座標を割り当て:

| グループID | 整列パターン | 基準座標(X, Y) | Sticky Note座標 | 色 | 配置方針 |
|-----------|------------|----------------|----------------|-----|---------|
| 全体フロー | 横一列 | (100, 100) | (100, 50) | 7（薄ピンク） | 上部固定 |
| G001 | 横一列 | (100, 400) | (50, 350) | 6（薄オレンジ） | 垂直配置 |
| G002 | 横一列 | (100, 900) | (50, 850) | 3（薄青） | 垂直配置・G001の下 |
| G003 | AI階層 | (100, 1400) | (50, 1350) | 4（薄紫） | 垂直配置・G002の下、AIサブノードぶら下げ |
| G004 | 横一列 | (100, 1900) | (50, 1850) | 2（薄緑） | 垂直配置・G003の下 |
| E001 | 横一列 | (100, 2400) | (50, 2350) | 5（薄赤） | 下部エラーフロー |

**並列グループの例**（同一レイヤー内）:
| グループID | 整列パターン | 基準座標(X, Y) | Sticky Note座標 | 色 | 配置方針 |
|-----------|------------|----------------|----------------|-----|---------|
| G002a | 横一列 | (100, 900) | (50, 850) | 3（薄青） | 並列処理1 |
| G002b | 横一列 | (700, 900) | (650, 850) | 3（薄青） | 並列処理2（右へ600px） |
| G002c | 横一列 | (1300, 900) | (1250, 850) | 3（薄青） | 並列処理3（右へ1200px） |

4. **視覚的整合性チェック**
- **グループ間の最小間隔**: 500px（Sticky Note高さ + マージン）
- **グループ内ノード間隔**:
  - パターンA（横一列）: 水平300px、垂直0px（同一Y座標）
  - パターンB（AI階層）: メインノードは水平300px、サブノードは親の下200px・水平150px間隔
  - パターンC（グリッド）: 水平300px、垂直180px
- **Sticky Noteマージン**: 上50px、左50px、右50px、下50px
- **Sticky Note包含確認**: グループ内すべてのノードが Sticky Note 範囲内に収まることを保証

**出力例**:
```markdown
## グループ整列配置戦略

### グループ配置マトリックス

| グループID | 整列パターン | 基準座標 | Sticky Note座標 | 色 | グループ間隔 |
|-----------|------------|----------|----------------|-----|------------|
| 全体フロー | 横一列 | (100, 100) | (100, 50) | 7 | - |
| G001 | 横一列 | (100, 400) | (50, 350) | 6 | 垂直300px（全体フローの下） |
| G002 | 横一列 | (100, 900) | (50, 850) | 3 | 垂直500px（G001の下） |
| G003 | AI階層 | (100, 1400) | (50, 1350) | 4 | 垂直500px（G002の下） |
| G004 | 横一列 | (100, 1900) | (50, 1850) | 2 | 垂直500px（G003の下） |
| E001 | 横一列 | (100, 2400) | (50, 2350) | 5 | 垂直500px（G004の下、エラーフロー） |

### 整列パターン適用数
- 横一列配置: 5グループ（全体フロー、G001, G002, G004, E001）
- AI階層配置: 1グループ（G003）
- グリッド配置: 0グループ

### 配置の基本方針
- **垂直配置**: すべてのグループは基本的に上から下へ垂直に配置（X座標=100固定）
- **グループ間隔**: 500px（Sticky Note高さを考慮した十分な間隔）
- **グループ内配置**: 横一列（水平300px間隔）がデフォルト、AIノードは階層配置

### 視覚的整合性
- 最小グループ間隔: 500px ✅
- Sticky Note包含確認: すべてのグループで確認済み ✅
- 色の多様性: 5色使用（7, 6, 3, 4, 2, 5） ✅
- データフロー方向: 上から下へ一貫 ✅
```

---

### ステップ1: 入力確認と配置戦略決定

**目的**: 統合ワークフローJSONを読み込み、配置戦略を決定

**処理内容**:

1. **ワークフローJSON読み込み**
   ```json
   {
     "nodes": [ /* Step150で統合されたノード配列 */ ],
     "connections": { /* 接続情報 */ }
   }
   ```

2. **ノード分類**
   - Triggerノード特定
   - レイヤー別ノード分類（L1-L12）
   - グループ別ノード分類（G001-G040）
   - エラーフローノード特定

3. **配置戦略決定**（ステップ0のグループ整列配置戦略に基づく）
   - **メインフローの配置方針**: グループ垂直配置（上から下へ依存関係順） + グループ内横一列整列
   - **エラーフローの配置方針**: 最下部に分離配置 + 横一列整列
   - **並列グループの配置方針**: 同一Y座標で水平方向に展開 + グループ間隔600px確保

**出力例**:
```markdown
## 配置戦略

### メインフロー配置
- グループ数: 25グループ (G001-G025)
- 配置方針: **グループ垂直配置**（上から下へ依存関係順）
- グループ内配置: **横一列整列**（水平300px間隔、同一Y座標）
- グループ間隔: 垂直500px
- 基準X座標: 100（すべてのグループで統一）

### エラーフロー配置
- エラーグループ数: 5グループ (E001-E005)
- 配置方針: 最下部に垂直配置 + 横一列整列
- グループ間隔: 垂直500px
- Y座標開始位置: メインフロー最大Y + 500px

### 並列処理配置（特殊ケース）
- 並列グループ: G003a, G003b, G003c（同一レイヤー内）
- 配置方針: 同一Y座標で水平方向に展開
- グループ間隔: 水平600px
- グループ内配置: 各グループとも横一列整列

### AI階層配置（特殊ケース）
- 対象: AI Agentノードを含むグループ
- メインノード配置: 横一列整列（標準）
- サブノード配置: AI Agentノードの下にぶら下げ
  - Y座標: 親ノード + 200px
  - X座標: 親ノードを中心に左右に展開（150px間隔）
```

---

### ステップ2: グループ別Y座標計算

**目的**: グループの依存関係に基づく垂直方向（Y座標）の配置計算

**処理内容**:

1. **グループ依存関係の特定**
   - Step040のグループ構成表から依存関係を抽出
   - 例: G001 → G002 → G003 → G004 → G005（順次依存）
   - 並列グループの特定: G003a, G003b, G003c（同一Y座標）

2. **Y座標計算式**
   ```
   基準Y座標 = 100
   グループ間隔 = 500
   サブノード追加高さ = 250（AI階層配置の場合）

   グループY[n] = 基準Y + (n-1) × グループ間隔 + サブノード追加高さ
   ノードY = グループY（同一グループ内のノードは同一Y座標）
   AI サブノードY = 親ノードY + 200
   ```

3. **計算例**
   ```
   全体フロー:      Y = 100
   G001 (入力):     Y = 400   (100 + 300 全体フローとのマージン)
   G002 (変換):     Y = 900   (400 + 500)
   G003 (AI処理):   Y = 1400  (900 + 500)
   ├─ メインノード: Y = 1400
   └─ サブノード:   Y = 1600  (1400 + 200)
   G004 (出力):     Y = 2150  (1400 + 500 + 250 サブノード高さ考慮)
   E001 (エラー):   Y = 2650  (2150 + 500)
   ```

4. **グループ内ノード配置**
   - 同一グループ内のノードは水平方向に300px間隔で配置
   - すべてのノードは同一Y座標（横一列）
   - 例: G001内に3ノード存在する場合
     - Node1: [100, 400]
     - Node2: [400, 400] (100 + 300)
     - Node3: [700, 400] (400 + 300)

**出力例**:
```json
{
  "groupYCoordinates": {
    "全体フロー": 100,
    "G001": 400,
    "G002": 900,
    "G003": 1400,
    "G004": 2150,
    "E001": 2650
  },
  "groupHeights": {
    "全体フロー": 150,
    "G001": 150,
    "G002": 150,
    "G003": 400,
    "G004": 150,
    "E001": 150
  }
}
```

---

### ステップ3: グループ別X座標計算

**目的**: グループごとの水平方向（X座標）の配置計算

**処理内容**:

1. **並列グループの特定**
   - 同一Y座標に複数グループが存在するケースを抽出
   - 並列処理グループのみX座標が異なる
   - 通常のグループはすべて基準X座標（100）で統一

2. **X座標計算式**
   ```
   基準X座標 = 100
   並列グループ間隔 = 600（Sticky Note幅 + マージン）

   通常グループ:   グループX = 基準X (100)
   並列グループ:   グループX[i] = 基準X + i × 並列グループ間隔
   ```

3. **通常グループ配置**（デフォルト）
   - すべてのグループはX座標 = 100で統一
   - グループ間は垂直方向に配置されるため、X座標の変更は不要
   - 例: G001, G002, G003, G004
     - すべて X = 100

4. **並列グループ配置**（特殊ケース）
   - 同一Y座標に複数グループが存在する場合のみX座標を変更
   - 例: Y=900に G003a, G003b, G003c が並列存在
     - G003a: X = 100
     - G003b: X = 700  (100 + 600)
     - G003c: X = 1300 (700 + 600)

5. **グループ内ノード配置**
   - 各ノードのX座標 = グループX + (ノードインデックス × 300)
   - 例: G001内に3ノード、グループX = 100
     - Node1: X = 100
     - Node2: X = 400 (100 + 300)
     - Node3: X = 700 (400 + 300)

6. **AI階層配置のX座標**
   - メインノード: 標準の横一列配置
   - サブノード: 親ノードを中心に左右に展開
   - 例: AI Agentノード（X=400）に4つのサブノード
     - Language Model: X = 250 (親 - 150)
     - Memory: X = 400 (親と同じ)
     - Tool 1: X = 550 (親 + 150)
     - Tool 2: X = 700 (親 + 300)

**出力例**:
```json
{
  "groupXCoordinates": {
    "全体フロー": 100,
    "G001": 100,
    "G002": 100,
    "G003": 100,
    "G004": 100,
    "E001": 100
  },
  "parallelGroups": {
    "Y=1100": {
      "G003a": 100,
      "G003b": 700,
      "G003c": 1300
    }
  }
}
```

---

### ステップ4: ノード位置の更新

**目的**: 計算されたX/Y座標を各ノードのpositionフィールドに反映

**処理内容**:

1. **ノード位置計算**
   - レイヤーY座標 + グループX座標 + グループ内オフセット
   - 各ノードに一意の座標を割り当て

2. **position更新**
   ```json
   {
     "id": "node_123",
     "name": "AI Agent",
     "type": "@n8n/n8n-nodes-langchain.agent",
     "position": [500, 1000],  // [X, Y]
     "parameters": { /* ... */ }
   }
   ```

3. **グループ内オフセット計算**
   - 同一グループ内でノードが重ならないように垂直オフセットを追加
   - グループ内インデックス × 180px

4. **全ノード更新**
   - nodes配列内の全ノードのpositionを更新
   - Sticky Noteも含めて更新

**実装例**:
```javascript
// 疑似コード
nodes.forEach(node => {
  const group = node._comment.group;      // 例: "G003"
  const nodeType = node.type;             // ノードタイプ
  const parentNode = node._comment.parentNode; // AI階層の親ノード
  const groupIndex = getGroupIndex(node, group);  // グループ内インデックス

  const baseY = groupYCoordinates[group];
  const baseX = groupXCoordinates[group];

  // AI階層のサブノードか判定
  if (parentNode) {
    // AI Agentのサブノードはぶら下げてY座標を200px下げる
    const parent = nodes.find(n => n.id === parentNode);
    const subNodeIndex = getSubNodeIndex(node, parentNode);

    node.position = [
      parent.position[0] + (subNodeIndex * 150) - 150, // 親を中心に左右展開
      parent.position[1] + 200  // 親の200px下
    ];
  } else {
    // 通常ノードは横一列配置
    node.position = [
      baseX + (groupIndex * 300),  // 水平方向に300px間隔
      baseY                         // 同一Y座標
    ];
  }
});
```

**出力例**:
```json
{
  "nodes": [
    {
      "id": "trigger_001",
      "name": "Webhook Trigger",
      "position": [100, 400],
      "_comment": { "group": "G001", "groupIndex": 0 }
    },
    {
      "id": "validation_001",
      "name": "Input Validation",
      "position": [400, 400],
      "_comment": { "group": "G001", "groupIndex": 1 }
    },
    {
      "id": "agent_001",
      "name": "AI Agent - User Query",
      "position": [100, 1400],
      "_comment": { "group": "G003", "groupIndex": 0 }
    },
    {
      "id": "langmodel_001",
      "name": "OpenAI Language Model",
      "position": [100, 1600],
      "_comment": { "group": "G003", "parentNode": "agent_001", "subNodeIndex": 0 }
    },
    {
      "id": "memory_001",
      "name": "Buffer Memory",
      "position": [250, 1600],
      "_comment": { "group": "G003", "parentNode": "agent_001", "subNodeIndex": 1 }
    },
    {
      "id": "output_001",
      "name": "Respond to Webhook",
      "position": [100, 2150],
      "_comment": { "group": "G004", "groupIndex": 0 }
    }
  ]
}
```

---

### ステップ5: 接続ライン最適化（オプション）

**目的**: ノード間の接続ラインが視覚的に明確になるよう配置を微調整

**処理内容**:

1. **接続パターン分析**
   - connectionsオブジェクトから接続関係を抽出
   - 交差する接続ラインを特定

2. **交差最小化調整**
   - 交差が発生する場合、ノードのX座標を微調整
   - 調整幅: ±50px 以内

3. **長距離接続の最適化**
   - レイヤーを跨ぐ接続（例: L1 → L5）の視覚的明確化
   - 中間ノードが存在する場合、配置を調整

**注意**: このステップはオプションです。基本配置で十分な場合はスキップ可能。

---

### ステップ6: Sticky Note位置調整

**目的**: グループタイトル用Sticky Noteを適切な位置に配置

**処理内容**:

1. **Sticky Note特定**
   - type: "@n8n/n8n-nodes-base.stickyNote"
   - グループタイトル用Sticky Noteを抽出

2. **配置ルール**
   - グループ全体を包含する位置とサイズに配置
   - Y座標: グループY座標 - 50px（グループの上部）
   - X座標: グループX座標 - 50px（グループの左側）
   - 幅: グループ内ノードの最大X座標 - 最小X座標 + 350px（マージン含む）
   - 高さ:
     - 通常グループ: 250px（ノード高さ100px + 上下マージン）
     - AI階層グループ: 500px（サブノード領域を含む）

3. **視認性・アクセシビリティルール**
   - Sticky Noteの外枠がグループノードの上に重ならないようにX/Yを微調整
   - `parameters.color` は color=0/1禁止。全体=7、メイン=6、エラー=5を維持
   - `parameters.width` / `height` はStep180の `calculateStickyDimensions()` で算出し、全文が表示されるサイズを設定
   - `parameters.content` には `nodes` ブロックで関連ノードを列挙し、位置と内容の一貫性を担保

4. **position更新**
   ```json
   {
     "id": "sticky_g001",
     "name": "📌 G001: 入力受付グループ",
     "type": "@n8n/n8n-nodes-base.stickyNote",
     "position": [50, 350],  // グループG001の上部・左側
     "parameters": {
       "content": "## G001: 入力受付グループ\n\n### ノードブロック\n```nodes\n- Webhook Trigger (webhook / webhook_g001_01)\n- Input Validation (set / set_g001_02)\n```\n\n...",
       "width": 750,   // ノード2つ分(100+300+240) + マージン
       "height": 250,  // 通常グループ高さ
       "color": 6
     }
   }
   ```

   **AI階層グループの例**:
   ```json
   {
     "id": "sticky_g003",
     "name": "📌 G003: AI処理グループ",
     "type": "@n8n/n8n-nodes-base.stickyNote",
     "position": [50, 1350],  // グループG003の上部・左側
     "parameters": {
       "content": "## G003: AI処理グループ\n\n### メインノード\n```nodes\n- AI Agent (agent / agent_g003_01)\n```\n\n### サブノード（AIエージェント階層）\n```nodes\n- OpenAI Language Model (langmodel / langmodel_g003_02)\n- Buffer Memory (memory / memory_g003_03)\n- Tool 1 (tool / tool_g003_04)\n```\n\n...",
       "width": 650,   // AI Agent + サブノード横展開分
       "height": 500,  // AI階層グループ高さ（サブノード領域含む）
       "color": 4
     }
   }
   ```

**出力例**:
```json
{
  "nodes": [
    {
      "id": "sticky_g001",
      "position": [50, 350],
      "parameters": {
        "width": 750,
        "height": 250,
        "color": 6
      },
      "_comment": { "group": "G001", "purpose": "通常グループタイトル" }
    },
    {
      "id": "sticky_g003",
      "position": [50, 1350],
      "parameters": {
        "width": 650,
        "height": 500,
        "color": 4
      },
      "_comment": { "group": "G003", "purpose": "AI階層グループタイトル" }
    }
  ]
}
```

---

### ステップ6.5: Sticky Note範囲計算とノード包含確認

**目的**: Sticky Noteの範囲内にグループ内の全ノードが配置されることを保証

**処理内容**:

1. **グループごとのノード範囲計算**
   ```javascript
   function calculateGroupBounds(group, nodes) {
     const groupNodes = nodes.filter(n => n._comment?.group === group);
     const minX = Math.min(...groupNodes.map(n => n.position[0]));
     const maxX = Math.max(...groupNodes.map(n => n.position[0]));
     const minY = Math.min(...groupNodes.map(n => n.position[1]));
     const maxY = Math.max(...groupNodes.map(n => n.position[1]));

     return { minX, maxX, minY, maxY };
   }
   ```

2. **Sticky Noteサイズ計算**
   ```javascript
   function calculateStickyNoteSize(groupBounds, nodeCount, hasAIHierarchy) {
     // 幅の計算（横一列配置）
     const width = Math.max(
       500,  // 最小幅
       (groupBounds.maxX - groupBounds.minX) + 350  // 実際の範囲 + マージン
     );

     // 高さの計算
     let height;
     if (hasAIHierarchy) {
       // AI階層グループ: サブノード領域を含む
       height = Math.max(
         500,  // 最小高さ（AI階層用）
         (groupBounds.maxY - groupBounds.minY) + 300  // 実際の範囲 + マージン
       );
     } else {
       // 通常グループ: 横一列のため高さは固定
       height = 250;  // ノード高さ100px + 上下マージン150px
     }

     return { height, width };
   }
   ```

3. **Sticky Note位置計算**
   ```javascript
   function calculateStickyNotePosition(groupBounds) {
     return [
       groupBounds.minX - 50,  // グループ左端より50px左
       groupBounds.minY - 150  // グループ上端より150px上
     ];
   }
   ```

4. **包含確認**
   ```javascript
   function verifyStickyNoteInclusion(stickyNote, groupNodes) {
     const stickyBounds = {
       minX: stickyNote.position[0],
       maxX: stickyNote.position[0] + stickyNote.parameters.width,
       minY: stickyNote.position[1],
       maxY: stickyNote.position[1] + stickyNote.parameters.height
     };

     const allIncluded = groupNodes.every(node => {
       return node.position[0] >= stickyBounds.minX &&
              node.position[0] + 240 <= stickyBounds.maxX && // ノード幅240px
              node.position[1] >= stickyBounds.minY &&
              node.position[1] + 100 <= stickyBounds.maxY;   // ノード高さ100px
     });

     return allIncluded;
   }
   ```

**出力例**:
```markdown
## Sticky Note包含確認結果

### G001: 入力受付グループ
- Sticky Note位置: [100, -150]
- Sticky Noteサイズ: 480px × 320px
- グループノード数: 2個
- 包含確認: ✅ すべてのノードが範囲内
- ノード範囲: X: 150-390, Y: 0-180

### G010: AI処理グループ
- Sticky Note位置: [100, 850]
- Sticky Noteサイズ: 600px × 500px
- グループノード数: 4個
- 包含確認: ✅ すべてのノードが範囲内
- ノード範囲: X: 150-490, Y: 1000-1540
```

**重要**: この計算により、Step180で生成されるSticky Noteの視覚的包含性が保証されます。

---

### ステップ7: 配置設計ドキュメント作成

**目的**: 配置ロジックと配置結果を文書化

**処理内容**:

1. **配置ルールの記述**
   - レイヤー配置ルール
   - グループ配置ルール
   - ノード間隔ルール

2. **配置マップ作成**
   - ASCII artで視覚的レイアウトを表現
   - 各グループの位置を示す

3. **配置統計**
   - レイヤー数、グループ数、ノード総数
   - X座標範囲、Y座標範囲
   - 最大グループサイズ

**出力例**:
```markdown
# 配置設計ドキュメント

## 配置ルール

### レイヤー配置
- 基準Y座標: 100
- レイヤー間隔: 300px
- 使用レイヤー: L1, L2, L3, L5, L7, L8, L9

### グループ配置
- 基準X座標: 100
- グループ間隔: 400px
- 並列グループ: L3に3グループ並列

### ノード間隔
- 垂直間隔: 180px（同一グループ内）
- 水平間隔: 400px（並列グループ間）

## 配置マップ

```
Y=100   [全体フロー（横長）]
        ┃
Y=400   [G001: Node1→Node2→Node3]
        ┃ ↓ 依存関係
Y=900   [G002: Node1→Node2→Node3→Node4]
        ┃ ↓ 依存関係
Y=1400  [G003: AI処理]
        │  Node1→AIAgent→Node3
        │       │
        │       ├─ Language Model (Y=1600)
        │       ├─ Memory (Y=1600)
        │       └─ Tool (Y=1600)
        ┃ ↓ 依存関係
Y=2150  [G004: Node1→Node2]
        ┃ ↓ 依存関係
Y=2650  [E001: エラー処理]

X=100   (すべてのグループの基準X座標)
        ノードは水平300px間隔で展開

並列グループの例（特殊ケース）:
Y=900   [G002a] [G002b] [G002c]
        X=100   X=700   X=1300
```

## 配置統計

- グループ数: 25グループ（メイン） + 5グループ（エラー）
- ノード総数: 87ノード
- 配置方針: グループ垂直配置 + グループ内横一列整列
- X座標範囲: 100 - 1500（通常グループ）、並列時は最大2000
- Y座標範囲: 100 - 3000
- グループ間隔: 500px（垂直方向）
- ノード間隔: 300px（水平方向、グループ内）
- 最大グループサイズ: 5ノード（横一列配置）
- AI階層グループ数: 3グループ（サブノード含む）
```

---

## 初回質問

Step180を開始する前に、以下を確認させてください:

### 1. 入力ファイルの確認

**質問**: Step170で出力されたSticky Note完成ワークフローJSONファイルのパスを教えてください。

**回答例**:
```
ファイルパス: ./output/workflow_with_stickynotes.json
```

---

### 2. 配置戦略の確認

**質問**: 以下の配置戦略で問題ありませんか？特別な配置要件がある場合は教えてください。

**デフォルト戦略**:
- **グループ垂直配置**（上から下へ依存関係順）
- **グループ内横一列整列**（水平300px間隔）
- **AI階層配置**（AIエージェント関連ノードは親の下にぶら下げ）
- エラーフローは最下部に分離配置
- グループ間隔: 垂直500px
- 並列グループのみ水平展開（水平600px間隔）

**追加要件例**:
- 「特定のグループを並列配置にしたい」
- 「AI階層配置を適用したいグループを指定」
- 「グループ間隔を調整したい（標準500px）」
- 「より密な配置にしたい（間隔を縮める）」

---

### 3. 視覚的出力の希望

**質問**: 配置結果の視覚的確認方法についてどれを希望しますか？

**選択肢**:
a) ASCII artレイアウト図（テキストベース）
b) 配置座標リスト（JSON形式）
c) 両方

**回答例**:
```
選択: c) 両方
```

---

### 4. カスタマイズ要件

**質問**: 以下のカスタマイズ要件はありますか？

**チェックリスト**:
- [ ] 特定のグループを異なる位置に配置したい
- [ ] ノード間隔を変更したい（標準: 縦180px、横400px）
- [ ] Sticky Noteの配置位置を変更したい
- [ ] エラーフローの配置位置を変更したい（デフォルト: 右側）

**回答例**:
```
- [x] ノード間隔を変更: 縦200px、横500pxにしたい
- [ ] その他は標準のまま
```

---

## 次のステップ

Step180完了後、最終ステップに進みます:

1. **Step190: 最終出力**
   - 配置最適化されたワークフローJSONの最終出力
   - メタデータJSON、README、実装ガイド作成
   - 検証レポート作成

---

## 補足情報

### 配置ベストプラクティス

1. **可読性優先**: 複雑な配置よりシンプルで理解しやすい配置を優先
2. **一貫性**: 同じルールを全グループに適用
3. **スケーラビリティ**: 将来のグループ追加を考慮した配置間隔
4. **データフロー可視化**: 上から下、左から右のフロー表現

### 参考: n8nキャンバスの特性

- ノードサイズ: 約240px × 100px
- 推奨ズームレベル: 100%
- キャンバススクロール: 自由にスクロール可能
- 接続ライン: 自動描画（n8nが制御）

---

**このプロンプトを実行すると、Step170の処理が開始されます。上記の初回質問に回答することで、最適な配置設計が実現されます。**
